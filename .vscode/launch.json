{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "clang++ debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/demo",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake ninja: build"            
        }
    ]
}